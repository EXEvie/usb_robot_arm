A simple user interface for controlling a Maplin USB robot arm.

ArmMove.py contains commands for connecting to the arm and moving each joint

arm_gui.py uses the commands from ArmMove in a tkinter interface to allow a user to move the arm via simple button presses